const nome = 'Daniel1'; // 'string'
const nome1 = 'Daniel2'; // "string"
const nome2 = `Daniel3`; // `string`
const num1 = 10; // number
const num2 = 10.52; // number
let nomeAluno; // undefined
const sobrenomeAluno = null; // nulo
const aprovado = false; // Boolean

console.log(
	typeof (nome, nome1, nome2),
	typeof (num1, num2),
	typeof nomeAluno,
	typeof sobrenomeAluno,
	typeof aprovado,
	nome,
	nome1,
	nome2,
	num1,
	num2,
	nomeAluno,
	sobrenomeAluno,
	aprovado,
);
