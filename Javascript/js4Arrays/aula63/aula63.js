const nomes = ['Daniel', 'Fernanda', 'Riven', 'Jhin', 'Kayn'];

nomes.splice(2, 0, 'Ordis', 'Clem');
console.log(nomes);

// nomes.splice(índice atual, delete, el1, el2, el3);

// pop
// const removidos = nomes.splice(-1, 1);

// shift
// const removidos = nomes.splice(0, 1);
